# AI-Powered-Occupational-Safety-Risk-Detection-System

1. Install the dependencies by running `pip install -r requirements.txt`
2. Train the AI by following `notebooks/train_yolov8.ipynb` in the repository
3. Type `streamlit run app/main.py` to launch the app. Optionally parameters can be configured by editing `app/config.py`
